-- Откат: удалить добавленные/обновлённые чанки по урокам 1–4 (полный откат не делаем — только убираем лишние индексы для уроков 2–4; урок 1 — только чанки 5–6)
-- Удаляем чанки 5, 6 у урока "Что такое SRE и зачем"
DELETE FROM lesson_content_chunks
WHERE (lesson_id, chunk_index) IN (
  SELECT l.id, v.chunk_index
  FROM lessons l
  JOIN modules m ON m.id = l.module_id AND m.slug = 'foundations'
  CROSS JOIN (VALUES (5), (6)) AS v(chunk_index)
  WHERE l.title = 'Что такое SRE и зачем'
);

-- Урок 2: оставляем один чанк (восстановить короткий текст вручную при необходимости)
-- Уроки 3, 4: оставляем по одному чанку
-- Для полного возврата к 0005 нужно перезапустить 0005 после отката 0006 — не делаем автоматически.
